# domain 계층 규칙

- 외부 라이브러리 import 금지. 표준 라이브러리 + dataclass만.
- DB, HTTP, LLM, 환경변수, 로깅 전부 금지.
- 모든 함수는 순수 함수. 전역 상태 없음.
- 함수를 추가하면 같은 커밋에 테스트를 추가한다.
- 데미지/실수치 관련 수정 시 골든 테스트를 먼저 돌리고 시작한다.
- 골든 테스트가 실패하면 기대값을 고치지 말고 구현을 고친다.
