# CLAUDE.md — pokeHelper 프로젝트 지침

이 문서는 Claude Code가 이 저장소에서 작업할 때 **항상 따라야 하는 규칙**이다.
작업을 시작하기 전에 이 문서를 먼저 읽고, 충돌하는 지시가 있으면 이 문서를 우선한다.

---

## 0. 프로젝트 한 줄 정의

> 포켓몬 챔피언스(Pokémon Champions) 실전배틀 유저를 위한 **파티 구축 + 선출 판단 보조 SaaS**.
> 모든 수치는 결정론적 계산 엔진이 만들고, LLM은 그 수치를 **해석·설명**하는 역할만 한다.

타깃 사용자는 "인터넷 파티를 복사해 쓰지만 왜 그런지 모르는 초보"와 "선출 판단을 빠르게 검증하고 싶은 중상급자"다.
따라서 **모든 출력에는 근거(수치·출처)가 붙어야 한다.** 근거 없는 추천은 이 서비스의 존재 이유를 부정한다.

---

## 1. 절대 원칙 (위반 시 코드 리뷰에서 반려)

### 1-1. LLM은 숫자를 만들지 않는다
- 데미지 %, 확정 n타, 스피드 실수치, 노력치 배분 — **전부 `app/domain/` 의 순수 함수가 계산한다.**
- LLM에게 "이 기술 몇 % 들어가?"를 묻는 코드는 절대 작성하지 않는다.
- LLM 프롬프트에는 **이미 계산된 수치를 입력으로 주입**하고, "이 수치를 근거로 설명하라"고만 지시한다.
- LLM이 응답에 포함시킨 숫자를 DB에 저장하거나 UI에 수치로 표시하지 않는다.

### 1-2. 데이터 출처를 항상 유지한다
- 사용률·샘플 데이터는 반드시 `source`(opgg / pokedb / manual), `collected_at`, `regulation_id`를 함께 저장한다.
- UI에 통계를 표시할 때는 출처와 수집 시각을 같이 노출한다.
- 출처가 없는 수치는 화면에 띄우지 않는다.

### 1-3. 추정과 사실을 구분한다
- 상대 파티 분석은 **"범용 샘플 가정"** 이 전제다. 이 전제를 응답 스키마에 `assumptions` 필드로 명시한다.
- "상대가 이 기술을 들고 있다"가 아니라 "상위 3개 샘플 중 X%가 이 기술을 채용"으로 표현한다.
- "카운터"라는 단어는 조건 없이 쓰지 않는다. 항상 `조건 + 무너지는 케이스`를 함께 반환한다.

### 1-4. 계산으로 나오는 값은 수집하지 않는다
- 외부 사이트에서 가져오는 것은 **"사람들이 무엇을 고르는가"** 뿐이다: 사용 순위/사용률, 아이템·성격·특성·기술 채용률, 정석 샘플 조합.
- 종족값, 기술 위력, 타입 상성, 실수치, 결정력, 내구력, 스피드 라인은 **전부 우리 엔진이 계산한다.** 절대 수집 대상이 아니다.
- 마스터 데이터(포켓몬/기술/특성/아이템 원본 수치)는 수집이 아니라 **프로젝트가 직접 관리하는 시드 데이터**(`backend/data/*.json`)로 둔다.
- 이유: 수집이 깨져도 계산 기능은 100% 살아있어야 한다. 외부 의존을 최소 표면적으로 줄인다.

### 1-5. 도메인 계층은 외부 의존성 0
- `app/domain/` 안의 코드는 DB, HTTP, LLM, 환경변수를 **import 하지 않는다.**
- 입력은 dataclass/Pydantic 모델, 출력은 dataclass. 순수 함수만.
- 이유: 계산 정확도가 이 서비스의 생명이고, 순수 함수여야 테스트로 100% 검증 가능하다.

### 1-6. 도메인 코드는 테스트 없이 머지 금지
- `app/domain/` 에 추가/수정하는 모든 함수는 대응하는 테스트가 있어야 한다.
- 데미지 계산은 **최소/최대 roll 둘 다** 테스트한다. 평균값만 테스트하는 건 무의미하다.

---

## 2. 기술 스택 (변경 시 반드시 먼저 논의)

| 레이어 | 선택 | 이유 |
|---|---|---|
| 백엔드 | Python 3.12 + FastAPI | 계산 엔진이 파이썬으로 가장 깔끔. 타입힌트 + Pydantic으로 스키마 강제 |
| DB | PostgreSQL 16 | 포켓몬/기술/샘플/사용률 관계형 데이터. JSONB로 가변 스펙 수용 |
| 캐시 | Redis 7 | LLM 응답 캐싱, 수집 데이터 캐싱, rate limit |
| ORM | SQLAlchemy 2.0 (async) + Alembic | 마이그레이션 추적 필수 |
| 프론트 | Next.js 15 (App Router) + TypeScript | SSR로 SEO(파티 공유 페이지), 타입 공유 |
| UI | Tailwind CSS + shadcn/ui | 빠른 구축, 커스터마이징 가능 |
| 상태관리 | TanStack Query | 서버 상태 캐싱이 대부분 |
| LLM | Anthropic Claude API (백엔드에서만 호출) | API 키 노출 방지 |
| 컨테이너 | Docker + Docker Compose | 로컬/운영 환경 동일화 |
| 배포 | AWS ECS Fargate + RDS + ElastiCache + ALB | 포트폴리오 목적, EKS 대비 운영 부담 낮음 |
| IaC | Terraform | 인프라 코드화 (포트폴리오 가치) |
| CI/CD | GitHub Actions | ECR push → ECS 배포 |

**금지 사항**
- 프론트엔드에서 Anthropic API를 직접 호출하지 않는다.
- ORM 없이 raw SQL을 흩뿌리지 않는다 (복잡한 집계 쿼리만 예외, `repositories/` 안에 격리).
- 도메인 로직을 API 라우터 안에 작성하지 않는다.

---

## 3. 디렉토리 구조

```
pokeHelper/
├── CLAUDE.md                    # 이 문서
├── docs/
│   ├── DEVELOPMENT_GUIDE.md     # 개발 가이드
│   ├── DOMAIN_GLOSSARY.md       # 용어집
│   └── adr/                     # 아키텍처 결정 기록
├── docker-compose.yml           # 로컬 개발
├── docker-compose.prod.yml      # 운영 검증용
├── .env.example
├── backend/
│   ├── Dockerfile
│   ├── pyproject.toml
│   ├── alembic/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/                # 설정, 로깅, 예외
│   │   ├── domain/              # ★ 순수 계산 — 외부 의존성 0
│   │   │   ├── models.py        # Pokemon, MoveSpec, BuildSpec 등 dataclass
│   │   │   ├── stats.py         # 실수치 계산
│   │   │   ├── typechart.py     # 타입 상성
│   │   │   ├── damage.py        # 데미지 계산 (핵심)
│   │   │   ├── indices.py       # 결정력 / 내구력 지표
│   │   │   ├── speed.py         # 스피드 비교
│   │   │   └── ev_solver.py     # 노력치 미세조정 탐색
│   │   ├── services/            # 유스케이스 조합
│   │   │   ├── matchup.py       # 기능2: 선출 3마리 추천
│   │   │   ├── team_builder.py  # 기능1: 파티 빌더
│   │   │   └── llm/             # 프롬프트 + 호출 + 캐시
│   │   ├── ingestion/           # 외부 데이터 수집 어댑터
│   │   │   ├── base.py          # StatsSource 인터페이스
│   │   │   ├── opgg.py
│   │   │   └── pokedb.py
│   │   ├── repositories/        # DB 접근만
│   │   ├── db/                  # 세션, 모델
│   │   └── api/v1/              # 라우터 — 얇게 유지
│   └── tests/
│       ├── unit/domain/         # ★ 가장 촘촘하게
│       ├── integration/
│       └── fixtures/
├── frontend/
│   ├── Dockerfile
│   └── src/
│       ├── app/
│       ├── components/
│       ├── lib/api/             # 백엔드 클라이언트 (타입 자동생성)
│       └── types/
└── infra/
    ├── terraform/
    └── scripts/
```

**의존 방향은 한 방향으로만 흐른다:**
`api → services → domain` / `api → services → repositories → db`
`domain` 은 아무것도 import 하지 않는다. 역방향 import를 발견하면 즉시 리팩터링한다.

---

## 4. 도메인 용어 (코드 네이밍 통일)

한국 서버 용어를 기준으로 하되, **코드 식별자는 영문**을 쓴다. UI 표시 문자열만 한글.

| 한글 (UI) | 코드 식별자 | 의미 |
|---|---|---|
| 실수치 | `actual_stat` | 종족값+개체값+노력치+성격 적용 후 최종 능력치 |
| 종족값 | `base_stat` | 포켓몬 고유 기본 수치 |
| 노력치 | `effort_points` | ★챔피언스 표기. 스탯당 0~32, 총합 66. 실수치 상승분 그대로 |
| 노력치 표기법 | `HABCDS` | UI 표준 표기. H=HP, A=공격, B=방어, C=특공, D=특방, S=스피드 (예: `A10C24S32 천진난만`) |
| 개체값 | `IV` | 챔피언스는 31 고정. 상수이므로 파라미터로 받지 않는다 |
| 성격보정 | `nature_modifier` | 1.1 / 1.0 / 0.9 |
| 자속 | `stab` | Same Type Attack Bonus (1.5) |
| 타입 상성 | `type_effectiveness` | 0 / 0.25 / 0.5 / 1 / 2 / 4 |
| 확정 1타 | `guaranteed_ohko` | 최소 roll로도 쓰러뜨림 |
| 난수 1타 | `chance_ohko` | 확률 값 함께 반환 (`ohko_chance: float`) |
| 확정 2타 | `guaranteed_2hko` | |
| 결정력 | `offense_index` | 커뮤니티 관용 지표 (표시 전용) |
| 내구력 | `bulk_index` | 커뮤니티 관용 지표 (표시 전용) |
| 선출 | `lead_selection` | 6마리 중 3마리 고르기 |
| 선봉 | `lead` | 선출 중 첫 번째로 내보내는 포켓몬 |
| 대면 | `matchup` | 1대1 대치 상황 |
| 사이클 | `cycle` | 교체 반복으로 이득 누적 |
| 전개 | `setup` | 벽/랭크업/날씨/필드 등 판 깔기 |
| 기점 | `setup_opportunity` | 상대가 전개를 시작할 수 있는 틈 |
| 샘플 | `build` / `build_sample` | 특정 포켓몬의 기술·아이템·노력치 조합 |
| 레귤레이션 | `regulation` | 시즌별 사용 가능 포켓몬 규정 |

전체 용어집은 `docs/DOMAIN_GLOSSARY.md`에 유지하고, 새 용어가 생기면 여기에 추가한 뒤 코드에 쓴다.

---

## 5. 계산 명세 (엔진의 진실의 원천)

### 5-0. 노력치 — 챔피언스는 포인트 방식 ★ (확정)

챔피언스는 **Lv.50 고정**이고, 노력치를 252가 아니라 **실제 상승하는 실수치 그대로** 표시한다.

**확정된 규칙**
| 항목 | 값 |
|---|---|
| 스탯당 최대 | **32** |
| 총 배분 가능 | **66** |
| 1포인트의 효과 | 실수치 그대로 +1 (성격 보정 전 기준) |
| 성격 보정 적용 시점 | **포인트를 더한 최종 실수치에 마지막으로 곱한다** |

- 기존 시리즈의 EV(0~252) 개념은 이 프로젝트에 **존재하지 않는다.** 변환 함수도 만들지 않는다.
- DB, API 요청/응답, UI — 전 구간에서 `effort_points` 하나로 통일한다. `252`, `EV` 라는 표현이 코드나 화면에 나오면 안 된다.
- 총합이 66이라 **32 + 32 + 2** 가 기본형이다 (기존 시리즈의 252/252/4에 대응). 나머지 2포인트를 어디에 꽂느냐가 미세조정의 출발점이 된다.

```python
# domain/effort.py
MAX_POINTS_PER_STAT: Final[int] = 32
MAX_TOTAL_POINTS: Final[int] = 66

def validate(spread: EffortSpread) -> None:
    """각 스탯 0~32, 총합 ≤ 66. 위반 시 InvalidEffortSpread 발생."""
```

이 검증은 **파티 저장 시점과 계산 진입 시점 양쪽에서** 호출한다. 불가능한 배분으로 계산한 수치를 사용자에게 보여주는 건 계산 오류보다 나쁘다.

### 5-1. 실수치 계산 (Lv.50 / 개체값 31 고정, 확정)

챔피언스는 **개체값이 31로 고정**이다. `2 × 종족값 + 31` 이 항상 홀수라 `floor(÷2)` 가 `종족값 + 15` 로 딱 떨어지고, 공식이 아래처럼 축약된다.

```
HP   = 종족값 + 75 + effort_points                          # 성격 보정 없음
기타 = floor( (종족값 + 20 + effort_points) * nature_mod )  # ★ 곱은 맨 마지막
```

- `nature_mod` ∈ {1.1, 1.0, 0.9}. **포인트를 더한 뒤에 곱하고 floor 한다.** 순서를 바꾸면 값이 어긋난다.
- `IV: Final[int] = 31` 을 `domain/constants.py` 에 상수로 두고, 축약 공식에 이미 반영돼 있음을 주석으로 명시한다.
- 뭉티기/질퍽이 등 HP 종족값 1 특수 케이스는 별도 처리

**검산 (한카리아스, 스피드 종족값 102)**

| 조정 | 계산 | 결과 |
|---|---|---|
| 최속 (상승보정 + 32) | floor((102+20+32) × 1.1) | 169 |
| 준속 (무보정 + 32) | 102+20+32 | 154 |
| 무보정 0 | 102+20 | 122 |
| 최저속 (하락보정 + 0) | floor(122 × 0.9) | 109 |

이 4개 라인을 `tests/unit/domain/test_stats.py` 의 첫 골든 케이스로 넣는다.

**설계상 중요한 귀결 두 가지**
1. 개체값 0으로 스피드를 낮추는 조정이 **불가능하다.** 저속 조정은 하락보정 + 0포인트가 하한이다. 트릭룸·자이로볼 관련 설계에서 기존 시리즈 감각을 그대로 가져오면 안 된다.
2. 상승보정 스탯은 1포인트가 최종 실수치를 1.1배 구간에서 움직이므로, 포인트 하나가 실수치 1을 못 올리는 구간이 생긴다. 따라서 **노력치 솔버의 판정 기준은 포인트가 아니라 항상 최종 실수치**다.

### 5-2. 데미지 계산

Lv.50 고정이라 앞 계수가 22로 고정된다.

```
기초 = floor( 22 * 위력 * 공격실수치 / 방어실수치 )
기초 = floor( 기초 / 50 ) + 2
```

**보정 적용 — 두 후보가 있다. A안을 기본으로 구현하고 골든 케이스로 확정한다.**

**A안 (기본값)** — 보정을 실수로 누적하고 맨 마지막에 한 번만 내림
```
mod = stab * type * weather * crit * ability * item * screen * burn
roll_i = floor( 기초 * mod * r_i )      for r_i in {0.85, 0.86, ..., 1.00}
```
이 경우 **보정끼리의 순서는 결과에 영향이 없다.**

**B안** — 본가 5세대 이후 방식. 단계마다 내림하고 난수를 자속/상성보다 먼저 곱한다.

두 안은 경계에서 ±1 어긋난다. 기초 100, 난수 0.85, 자속 1.5, 약점 2배인 경우:

| 안 | 계산 | 결과 |
|---|---|---|
| A | floor(100 × 3.0 × 0.85) | **255** |
| B | floor(100×0.85)=85 → floor(85×1.5)=127 → ×2 | **254** |

상대 HP가 255라면 A안은 확정 1타, B안은 난수 1타다. **답이 뒤집힌다.**

**왜 A안이 기본값인가**: 챔피언스 전용 공식 정리 문서가 최종 데미지를 `floor(보정데미지 × 난수)` 하나로만 표기한다. 챔피언스는 본가와 별개 엔진이므로 본가 규칙을 그대로 가정하지 않는다. 다만 해당 문서는 비공식 팬 자료이므로 **실측 검증 전까지 확정이 아니다.**

**구현 규칙**
- 보정 적용은 `domain/damage.py` 의 단일 함수 `apply_modifiers()` 안에서만 일어난다. **A↔B 전환이 이 함수 하나 수정으로 끝나야 한다.** 다른 곳에 보정 곱셈이 흩어지면 안 된다.
- 난수는 평균값 하나가 아니라 **0.85~1.00의 16단계를 전부 계산해서 리스트로 반환**한다.
- 확정타 판정은 16개 roll 중 상대 HP 실수치 이상인 개수로 한다.
  - 16/16 → 확정 1타
  - 1~15/16 → 난수 1타 (N/16 = 확률)
  - 0/16 → 2타 이상. 같은 방식으로 누적 판정
- `poke_round`(정확히 .5면 내림)는 **B안을 채택할 경우에만** 필요하다. A안이면 만들지 않는다. 어느 쪽이든 파이썬 기본 `round()`는 은행가 반올림이라 사용 금지.
- 검증 결과는 `docs/adr/003-damage-chain.md` 에 기록한다.

**골든 케이스 수집 팁**: 보정이 하나뿐인 케이스는 A/B가 같은 값을 내서 검증이 안 된다. **자속 + 약점 + 급소처럼 배율이 3개 이상 겹치는 케이스를 우선 수집**한다.

- 반환 타입은 항상 아래 구조:

```python
@dataclass(frozen=True)
class DamageResult:
    rolls: tuple[int, ...]          # 16개
    min_percent: float
    max_percent: float
    ko_summary: str                 # "확정 2타" / "난수 1타 (43.8%)"
    ohko_chance: float              # 0.0 ~ 1.0
    hits_to_ko_min: int
    hits_to_ko_max: int
    applied_modifiers: dict[str, float]   # ★ 어떤 보정이 걸렸는지 전부 노출
```

`applied_modifiers` 는 필수다. 사용자에게 "왜 이 숫자가 나왔는지" 설명하려면 보정 내역이 있어야 한다.

### 5-3. 결정력 / 내구력 (표시 전용 보조 지표)

커뮤니티에서 통용되는 근사 지표. **엔진의 정확한 계산을 대체하지 않는다.**

```
결정력(offense_index) ≈ 공격실수치 × 기술위력 × 자속보정 × 기타보정
내구력(bulk_index)    ≈ HP실수치 × 방어실수치 / K
평균 데미지% ≈ (결정력 / 내구력) × 타입상성
```

- `K` 는 커뮤니티 통용 상수(0.411 계열)를 쓰되, **Phase 1에서 정확한 데미지 계산 결과와 대조해 캘리브레이션한다.**
- 이 지표는 "빠른 감 잡기"용 UI 보조 표시로만 쓰고, 확정n타 판정에는 절대 사용하지 않는다.
- 근사값임을 UI에 명시한다.

### 5-4. 노력치 미세조정 솔버 (`ev_solver.py`)

사용자 제약조건을 받아 만족하는 노력치 배분을 탐색한다.

제약조건 타입:
- `SURVIVE`: 특정 상대의 특정 기술을 확정 n타로 버틴다
- `OHKO` / `NHKO`: 특정 상대를 확정 n타로 잡는다
- `OUTSPEED`: 특정 상대의 특정 스피드 라인을 +1 이상으로 추월한다

탐색 방식:
1. 탐색 단위는 **1 포인트**, 스탯당 0~32, **총합 66 이하**가 하드 제약이다.
2. 제약을 만족하는 최소 투자 포인트를 이진 탐색으로 찾는다 (스탯당 33개 값이라 탐색 공간이 작다 — 완전 탐색도 가능한 규모)
3. 판정은 포인트가 아니라 **최종 실수치**로 한다. 상승보정 스탯은 포인트 1이 실수치 1을 못 올리는 구간이 있다.
4. 남은 포인트는 사용자가 지정한 우선순위(결정력 / 물리내구 / 특수내구)에 분배
5. 제약을 전부 만족하면서 총합 66을 넘지 않는 해가 없으면 **불가능하다고 명확히 반환한다.** 어떤 제약끼리 충돌하는지도 함께 알려준다. 근사치를 정답인 척 내놓지 않는다.
6. 반환 시 "이 포인트가 무엇을 위한 것인지"를 제약조건과 함께 돌려준다 — 사용자가 배워야 하는 건 숫자가 아니라 근거다.

---

## 6. 코딩 규칙

### Python
- Python 3.12, 전 함수 타입힌트 필수
- 포매터/린터: `ruff` (format + lint), 타입체크: `mypy --strict` (domain 한정)
- 도메인 모델은 `@dataclass(frozen=True)` — 불변 객체
- 예외는 `app/core/exceptions.py` 에 정의한 도메인 예외를 쓴다. 맨 `Exception` 금지
- 매직 넘버 금지. 상수는 `app/domain/constants.py` 에 이름을 붙여 정의

### TypeScript
- `strict: true`
- 백엔드 스키마는 OpenAPI → `openapi-typescript` 로 자동 생성. **손으로 타입을 다시 쓰지 않는다.**
- 컴포넌트는 서버 컴포넌트 기본, 상호작용 필요할 때만 `"use client"`

### 테스트
- `pytest`, 도메인 커버리지 90% 이상 목표
- 데미지 계산은 **실제 게임/검증된 계산기와 대조한 골든 케이스**를 `tests/fixtures/golden_damage.json` 에 축적
- 골든 케이스가 깨지면 절대 테스트 기대값을 수정해서 통과시키지 않는다. 엔진을 고친다.

### 커밋
```
feat(domain): 데미지 계산에 화상 보정 추가
fix(ingestion): op.gg 사용률 파싱 시 폼체인지 포켓몬 중복 제거
test(domain): 확정2타 경계 케이스 골든 테스트 추가
```
- 한 커밋 = 한 논리적 변경
- 도메인 변경 커밋에는 반드시 테스트가 포함되어야 한다

---

## 7. LLM 연동 규칙

### 호출 구조
```
사용자 요청
  → domain 계산 (데미지/스피드/상성 전부 미리 계산)
  → 계산 결과를 구조화된 JSON으로 직렬화
  → LLM 프롬프트에 "사실 블록"으로 주입
  → LLM은 해석/전략/설명만 생성
  → 응답 스키마 검증 (Pydantic)
  → 캐시 저장
```

### 프롬프트 작성 규칙
- 프롬프트는 코드 안에 문자열로 박지 말고 `app/services/llm/prompts/*.md` 파일로 분리한다
- 모든 프롬프트에 다음 문장을 포함시킨다:
  > 제공된 계산 결과 블록에 없는 수치는 절대 생성하지 마라. 필요한 수치가 없으면 "계산되지 않음"이라고 답하라.
- 출력은 자유 텍스트가 아니라 **JSON 스키마 강제**. 파싱 실패 시 1회 재시도 후 실패 처리

### 비용 관리
- 동일 입력(파티 해시 + 상대 파티 해시 + 레귤레이션 버전)은 Redis에 캐싱, TTL 7일
- 토큰 사용량을 요청별로 로깅 (`llm_usage` 테이블)
- 사용자별 일일 호출 한도 (무료 티어 기준)

---

## 8. 데이터 수집 규칙

### 8-1. 주 데이터 소스: championsbattledata.com 공개 API

스크래핑하지 않는다. 공식 공개 JSON API를 쓴다.

| 엔드포인트 | 용도 |
|---|---|
| `GET /api` | 인덱스. 포켓몬 목록, Showdown ID, 사용 가능한 스냅샷 폴더, 포맷, 요약 |
| `GET /api/metadata/:name` | 폼별 종족값·타입·특성 → **마스터 시드 부트스트랩** |
| `GET /api/battle/:format/:name` | 기술·도구·성격·노력치 배분·특성·파트너 채용률 행 |
| `GET /api/battle/:format/:name?season=&days=` | 일별 스냅샷 (days 1~31) |

- 포맷은 `Singles` / `Doubles`. 이 서비스는 **싱글 기준**이다.
- 라우트 이름은 Showdown 내부 ID(`garchomp`, `raichualola`)를 쓴다. `pokemon` 테이블에 `showdown_id` 컬럼을 두고 매핑한다.
- 폼 구분은 `saved_name`, 폼 묶음은 `base_name` 을 쓴다. 우리 `pokemon.form_key` 와 매핑 테이블을 만든다.
- CORS가 열려 있지만 **프론트에서 직접 호출하지 않는다.** 백엔드가 호출하고 캐싱한다. 출처 표기와 요청량 통제를 한 곳에서 하기 위해서다.

### 8-2. 반드시 지켜야 할 라이선스 조건 ★

이 조건들은 협상 대상이 아니다. 위반하면 서비스를 내려야 한다.

- **출처 표기 필수.** 데이터를 표시하는 모든 화면에 "Battle data provided by Pokémon Champions Battle Data" 와 링크를 넣는다. 푸터 한 줄로 끝내지 말고, 통계가 나오는 화면마다 표기한다.
- **영구 미러/아카이브 금지.** 캐싱은 허용되지만 캐시가 원본의 영구 대체재가 되면 안 된다. → **보존 기간을 둔 캐시로 설계하고, 무한 누적 스냅샷 테이블을 만들지 않는다.**
- **경쟁 API·덤프·대량 다운로드 서비스 금지.** 우리 API가 이 데이터를 그대로 반환하는 공개 엔드포인트를 노출하면 안 된다. 우리 API는 **계산 결과와 분석**을 반환하고, 원본 통계는 UI 표시에 필요한 만큼만 실어 보낸다.
- **원본 데이터를 우리 것처럼 표시 금지.**
- **공식 제휴 아님 표기.** 포켓몬 관련 상표권 주장 금지.
- 고정 rate limit은 없지만 과도한 부하 시 차단될 수 있다. → 배치 요청 간 간격을 두고 동시성을 제한한다.

### 8-3. 수집 대상 (이것만)
| 수집함 | 수집 안 함 (엔진이 계산) |
|---|---|
| 포켓몬 사용 순위 / 사용률 | 실수치, 결정력, 내구력 |
| 아이템·성격·특성·기술 채용률 | 스피드 라인 (최속/준속/무보정/최저속) |
| 노력치 배분 채용률 | 데미지 %, 확정 n타 |
| 파트너(파티 동반) 채용률 | 타입 상성 판정 |
| 종족값·타입·특성 목록 (metadata) | 기술 조합 시너지 |

기술 위력·명중·PP·플래그는 API가 제공하지 않으므로 `backend/data/moves.json` 에 직접 관리한다.

### 8-4. 한영 명칭 매핑 규칙 ★

영문 API → 한국 서버 명칭 변환은 이 프로젝트에서 **오류 허용치가 0인 유일한 구간**이다.
설계 근거는 `docs/DEVELOPMENT_GUIDE.md` 4-5 참조. 코드에서 지킬 것:

- **퍼지/유사도 매칭 금지.** 정확 일치 사전 조회만. 유사도 임계값이 들어간 코드는 리뷰에서 반려한다.
- **문자열이 아니라 ID로 조인**한다. 영문명끼리 비교하지 않는다.
- 매핑은 `backend/data/name_map.json` 에 커밋해 고정한다. **런타임에 외부 사전 API를 호출하지 않는다.**
- 폼 명칭은 패턴을 런타임 조합하지 않는다. **검수된 확정 문자열만** 매핑에 넣는다.
- **종족값 교차검증을 반드시 통과시킨다.** 매핑한 포켓몬의 종족값이 API metadata와 다르면 매핑 오류로 처리한다.
  기술은 타입+위력+분류로 검증한다.
- `en→ko`, `ko→en` 양방향 유일성을 검사한다. 중복은 에러다.
- **매핑 실패 시 fail-closed.** 영문명을 통과시키거나 추측하거나 항목만 건너뛰지 않는다.
  스냅샷을 활성화하지 않고 `name_map_pending.json` 에 원문을 적재한 뒤 보고한다.
- 커버리지 테스트가 미매핑 하나라도 발견하면 빌드를 깬다.

### 8-5. 폴백
API가 내려가거나 응답이 비면:
1. 보존 기간 내 캐시를 그대로 쓴다 (마지막 갱신 시각을 UI에 표시)
2. 캐시도 없으면 **메타 없이 동작한다.** 상대 스펙은 "무보정 가정"으로 폴백하고, 가정임을 응답 `assumptions` 에 명시한다
3. 계산 기능은 어떤 경우에도 정상 동작해야 한다

op.gg / champs.pokedb.tokyo 스크래핑은 **보조 소스로 남겨두되 Phase 2 범위에서 제외**한다. 공개 API가 있는데 스크래핑을 먼저 만들 이유가 없다.

### 8-6. 구현 규칙
- 수집기는 `ingestion/base.py` 의 `StatsSource` 인터페이스를 구현한다. 사이트별 구현을 서비스 계층에 노출하지 않는다.
- **요청 간 최소 1초 지연, 동시성 1.** 고정 rate limit이 없다고 몰아치지 않는다.
- 공개 API를 우선한다. HTML 스크래핑은 최후 수단이다.
- 수집 원본(JSON/CSV)은 보존 기간 내에서만 보관한다.
- 수집 결과는 서비스 내부 스키마로 정규화한 뒤 저장한다. 사이트별 필드명을 DB에 그대로 넣지 않는다.

---

## 9. Claude Code 작업 방식

### 작업 시작 전
1. 관련 파일을 먼저 읽는다. 추측으로 작성하지 않는다.
2. 변경 범위가 3파일 이상이거나 도메인 계층을 건드리면 **먼저 계획을 제시하고 승인을 받는다.**
3. 기존 코드의 컨벤션을 따른다. 새 라이브러리 도입은 사전 논의.

### 작업 중
- 한 번에 하나의 기능만 구현한다. "겸사겸사" 리팩터링 금지.
- 도메인 함수를 만들면 같은 턴에 테스트도 만든다.
- 확실하지 않은 게임 사양(보정 순서, 특성 처리, 레귤레이션 규칙)은 **추측해서 구현하지 말고 질문한다.**

### 작업 후
- `ruff check`, `mypy`, `pytest` 를 실행하고 결과를 보고한다.
- 변경 요약에 "이 변경으로 깨질 수 있는 것"을 함께 적는다.

### 하지 말 것
- 포켓몬/기술 데이터를 파이썬 파일에 하드코딩 (→ DB 또는 `data/*.json`)
- 테스트 기대값을 바꿔서 실패를 통과시키기
- `.env`, 시크릿, API 키를 커밋
- 마이그레이션 없이 DB 모델 변경
- LLM 응답을 검증 없이 그대로 클라이언트에 전달
- 한 커밋에 백엔드+프론트+인프라를 동시에 섞기

---

## 10. 현재 단계

> 이 섹션은 진행에 따라 갱신한다.

- [ ] **Phase 0** — 저장소/Docker/CI 뼈대
- [ ] **Phase 1** — 도메인 계산 엔진 + 테스트 ★최우선
- [ ] **Phase 2** — 데이터 수집 + DB
- [ ] **Phase 3** — 기능2(선출 추천) API + 최소 프론트
- [ ] **Phase 4** — LLM 설명 레이어
- [ ] **Phase 5** — 기능1(파티 빌더)
- [ ] **Phase 6** — AWS 배포

상세 내용은 `docs/DEVELOPMENT_GUIDE.md` 참조.
